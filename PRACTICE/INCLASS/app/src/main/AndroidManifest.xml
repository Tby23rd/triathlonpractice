<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.class5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcherchicago"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcherchicago_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Class5">
        <activity android:name=".ArtInstituteActivity"></activity>
        <activity android:name=".NavyPierActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>